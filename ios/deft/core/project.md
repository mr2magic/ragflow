# Ragion Project Guidelines

Legend (from RFC2119): !=MUST, ~=SHOULD, ≉=SHOULD NOT, ⊗=MUST NOT, ?=MAY.

Only specify items here that **override or extend** the deft defaults.

**Reference**: `deft/main.md` | `deft/languages/swift.md`

## Project Configuration

**Tech Stack**: iOS Mobile App using Swift — SwiftUI, GRDB (SQLite), Keychain, Claude AI, OpenAI, Ollama

## Strategy

Use [brownfield](deft/strategies/brownfield.md) for this project.

## Workflow

```bash
task check         # Pre-commit gate
task test:coverage # Coverage target
task build         # Build project
task clean         # Clean artifacts
```

## Standards

**Quality:**
- ! Run `task check` before every commit
- ! Achieve ≥85% coverage overall + per-module
- ! Store secrets in `secrets/` dir
- ~ Provide `.example` templates for secrets

## Project-Specific Rules

- ⊗ Modify existing working Swift files unless explicitly required by the task
- ! Analyze existing codebase before proposing changes (brownfield strategy)
- ~ Prefer additive changes over rewrites

---

**Generated by**: deft-setup skill
**Date**: 2026-04-07
